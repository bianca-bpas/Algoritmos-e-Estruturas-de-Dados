// g++ -std=c++11 -O2 -Wall test.cpp -o test 
// g++ (versão) (otimizar) (show warnings) (source) -o (binary file)
#include <bits/stdc++.h>
#define endl "\n"
#define REP(i,a,b) for (int i = a; i <= b; i++)
#define SQ(a) (a)*(a)
typedef long long ll;
typedef vector<int> vi;
typedef pair<int, int> pi;
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    string s;
    getline(cin, s);

    int x;
    while (cin >> x){
        cout << "Received" << endl;
    }

    //freopen("input.txt", "r", stdin);
    //freopen("output.txt", "w", stdout);

    // int = 32-bit (-2.10^9...2.10^9)
    // long long = 64-bit (-9.10^18...9.10^18)
    // double = 64-bit (-9.10^18...9.10^18)
    // long double = 80-bit


    long long y = 123456789123456789LL;

}