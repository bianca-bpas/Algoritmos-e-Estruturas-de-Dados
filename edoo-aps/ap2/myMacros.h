// myMacros.h
#ifndef MY_MACROS_H
#define MY_MACROS_H

// Macro to compute the absolute value of a number
#define ABS(x) ((x) < 0 ? -(x) : (x))

// Macro to determine the greater of two numbers
#define MAX(a, b) ((a) > (b) ? (a) : (b))

#endif // MY_MACROS_H
